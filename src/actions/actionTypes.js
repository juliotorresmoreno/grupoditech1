// @flow

const _action_products = '@PRODUCTS';
export const action_products = {
    set_products: `${_action_products}/set_products`
}

const _action_recommends = '@RECOMMENDS';
export const action_recommends = {
    set_recommends: `${_action_recommends}/set_recommends`
}

const _action_store = '@STORE';
export const action_store = {
    buy: `${_action_store}/buy`
}